[package]
name = "counter"
version = "0.1.0"
edition = "2021"

[dependencies]
qubit = { path = "../../" }

serde = { version = "1.0", features = ["derive"] }
futures = "0.3.31"

tokio = { version = "1.44", features = ["full"] }
axum = "0.8"
hyper = { version = "1.6", features = ["server"] }
